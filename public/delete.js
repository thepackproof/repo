const tiktokCard=document.querySelector('#tiktok-delete-card');if(tiktokCard&&window.PACKPROOF_CONFIG?.tiktokEnabled)tiktokCard.hidden=false;const form=document.querySelector('#delete-form');const status=document.querySelector('#status');form?.addEventListener('submit',async(event)=>{event.preventDefault();const button=form.querySelector('button');button.disabled=true;status.textContent='Submitting securely…';try{const response=await fetch('/api/request-deletion',{method:'POST',headers:{'Content-Type':'application/json'},body:JSON.stringify({email:new FormData(form).get('email')})});const data=await response.json();status.textContent=data.message||'Request submitted.'}catch{status.textContent='The request could not be submitted. Try again later.'}finally{button.disabled=false}});
