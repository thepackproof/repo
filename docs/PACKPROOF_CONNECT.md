# PackProof Connect

This name is retired. The partner product is **PackProof API** (formerly PackProof Connect).

See [`PACKPROOF_API.md`](PACKPROOF_API.md).

Technical identifiers still use Connect (`/v1/connect/sessions`, `/api/connect/orders`, `/connect/capture`, `@packproof/connect`, `PackProofConnect`, `connectSessions`, `User-Agent: PackProof-Connect/1.0`, and related callables). The PackProof Button stays a separate `PAGE_DECLARED` path.
